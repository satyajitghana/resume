% One-page build — compact sizing, kestrel watermark, \verbose{} content dropped.
% Build:  cd src && tectonic resume-onepager.tex --outdir ../build
\documentclass[11pt]{article}
\def\ONEPAGER{}     % compact mode + drop \verbose{} bullets (see style.tex)
\input{style.tex}
\input{resume-body.tex}
