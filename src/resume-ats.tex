% ATS build — identical text, no background watermark.
% Build:  cd src && tectonic resume-ats.tex --outdir ../build
\documentclass[11pt]{article}
\def\ATSMODE{}      % suppresses the kestrel watermark in style.tex
\input{style.tex}
\input{resume-body.tex}
